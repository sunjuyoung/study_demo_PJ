server:
  port: 8081
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/shop
    username: root
    password: rnrdj123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        show_sql: true
        format_sql: true

